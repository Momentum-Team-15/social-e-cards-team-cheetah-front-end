export const Header = () => {
  return (
    <header>
      <button className='button-1'>Log In</button>
    </header>
  )
}

// this will render with a conditional in the main app.js I think OR there could be a check in the header component that determines which buttons (header) render
